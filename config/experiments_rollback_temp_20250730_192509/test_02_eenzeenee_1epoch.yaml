# eenzeenee T5 1 epoch 테스트 (RTX 3090 24GB 최적화)
experiment_name: eenzeenee_t5_1epoch_test
description: "eenzeenee T5 1 epoch 빠른 테스트 - RTX 3090 최적화"

general:
  model_name: eenzeenee/t5-base-korean-summarization
  data_path: ../data/
  train_path: data/train.csv
  val_path: data/dev.csv
  model_type: seq2seq
  name: eenzeenee_t5_1epoch_test

input_prefix: "summarize: "

tokenizer:
  bos_token: <pad>
  eos_token: </s>
  encoder_max_len: 256    # 테스트용 짧은 길이
  decoder_max_len: 32     # 테스트용 짧은 길이
  special_tokens:
    - '#Person1#'
    - '#Person2#'
    - '#Person3#'
    - '#PhoneNumber#'
    - '#Address#'
    - '#PassportNumber#'

# 1 epoch 테스트 설정
training:
  do_eval: true
  do_train: true
  
  # 1 epoch 설정
  num_train_epochs: 1                 # 1 epoch만
  max_steps: 150                      # 최대 150 스텝
  
  evaluation_strategy: steps
  eval_steps: 30                      # 자주 평가
  
  # RTX 3090 테스트용 배치 설정
  per_device_train_batch_size: 8      # eenzeenee는 더 큰 배치 가능
  per_device_eval_batch_size: 16
  gradient_accumulation_steps: 1
  
  # 학습 파라미터
  learning_rate: 3.0e-05
  lr_scheduler_type: linear
  warmup_steps: 15                    # 적은 웜업
  weight_decay: 0.01
  
  # 최적화 설정
  fp16: true
  gradient_checkpointing: false       # 작은 모델이므로 비활성화
  dataloader_num_workers: 4
  dataloader_pin_memory: true
  group_by_length: true
  
  # 저장 및 로깅
  logging_steps: 15                   # 자주 로깅
  save_strategy: steps
  save_steps: 75
  save_total_limit: 1
  load_best_model_at_end: false
  
  # 생성 설정
  predict_with_generate: true
  generation_num_beams: 2
  generation_max_length: 32
  
  report_to: []                       # WandB 비활성화 (테스트)
  seed: 42

wandb:
  entity: lyjune37-juneictlab
  project: nlp-5
  name: eenzeenee_t5_1epoch_test_lyj
  tags: [eenzeenee, T5, 1epoch, test, RTX3090, lyj]
  notes: "eenzeenee T5 1 epoch 빠른 테스트"

# Generation settings
generation:
  max_length: 256
  min_length: 5
  num_beams: 4
  no_repeat_ngram_size: 2
\ntraining:\n  eval_strategy: no
